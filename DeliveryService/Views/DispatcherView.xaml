<Window x:Class="DeliveryService.Views.DispatcherView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:DeliveryService.Views"
        mc:Ignorable="d"
        Title="DispatcherView" Height="800" Width="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#F0F0F0">
            <wv2:WebView2 x:Name="Map"/>
        </Border>
        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0" Grid.Column="1">
            <StackPanel Margin="15,20,15,20">
                <TextBlock Text="Активные заказы: " 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           Foreground="Black"
                           Margin="0,0,0,10"/>

                <ListView x:Name="ActiveOrdersListView" BorderThickness="0" Background="Transparent" Height="300">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCC" BorderThickness="1"
                                    CornerRadius="4" Padding="10" Margin="0,0,0,5"
                                    Background="LightGray">
                                <TextBlock Style="{StaticResource TextBlockStyle}" 
                                           Text="{Binding OrderName}" 
                                           FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="Курьеры онлайн: " 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           Foreground="Black"
                           Margin="0,0,0,10"/>

                <ListView x:Name="CouriersListView" BorderThickness="0" Background="Transparent" Height="300">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCC" BorderThickness="1" 
                                    CornerRadius="4" Padding="10" Margin="0,0,0,5"
                                    Background="LightGray">
                                <Grid>
                                    <TextBlock Text="{Binding Name}" 
                                               Style="{StaticResource TextBlockStyle}"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding Status}" 
                                               FontSize="14"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</Window>