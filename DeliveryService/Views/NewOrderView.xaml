<UserControl x:Class="DeliveryService.Views.NewOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="400">

    <Grid Background="#F9FAFB">
        <Border Background="White" CornerRadius="10" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <StackPanel>
                <TextBlock Text="Новый заказ" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="Black"/>

                <TextBlock Text="Номер заказа" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="№ 2026-0001" IsReadOnly="True" Background="#F3F4F7" Foreground="Gray" FontWeight="Bold"/>

                <TextBlock Text="Имя клиента" Style="{StaticResource FieldLabel}"/>
                <TextBox />

                <TextBlock Text="Телефон" Style="{StaticResource FieldLabel}"/>
                <TextBox />

                <TextBlock Text="Адрес доставки" Style="{StaticResource FieldLabel}"/>
                <TextBox />

                <Button Content="Создать заказ" Style="{StaticResource PrimaryButton}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>