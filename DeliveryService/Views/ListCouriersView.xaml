<Window x:Class="DeliveryService.Views.ListCouriersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeliveryService.Views"
        mc:Ignorable="d"
        Title="ListCouriersView" Height="800" Width="1100">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0" Margin="0,0,0,20" Cursor="">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Курьеры" FontSize="28" FontWeight="SemiBold" Foreground="#111827"/>

            <Button Content="Добавить курьера" Margin="785,0,0,0" Padding="20,0" Height="40" Background="#2563EB" Foreground="White" BorderThickness="0" Cursor="Hand" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>


        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>


            <Border Background="White" CornerRadius="12" 
        BorderBrush="#E5E7EB" BorderThickness="1"
        Width="180" Height="100" Margin="0,0,10,0">
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Всего заказов" Foreground="#6B7280" FontSize="13" Margin="10,0,0,0"/>
                    <TextBlock Text="13" FontSize="32" FontWeight="Bold" Foreground="#111827" Margin="10,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Онлайн сейчас -->
            <Border Grid.Column="1" Padding="35,0,35,0" CornerRadius="12" 
        BorderBrush="#E5E7EB" BorderThickness="1"
        Width="180" Height="100" Margin="0,0,10,0">
                <StackPanel Margin="-36,0,-36,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Онлайн сейчас" Foreground="#6B7280" FontSize="13" Margin="10,0,0,0"/>
                    <TextBlock Text="1" FontSize="32" FontWeight="Bold" Foreground="#10B981" Margin="10,5,0,0"/>
                </StackPanel>
            </Border>


            <Border Grid.Column="2" Padding="35,0,0,0" CornerRadius="12" 
        BorderBrush="#E5E7EB" BorderThickness="1"
        Width="180" Height="100" Margin="0,0,10,0">
                <StackPanel Margin="-36,1,-1,-1" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Офлайн" Foreground="#6B7280" FontSize="13" Margin="10,0,0,0"/>
                    <TextBlock Text="1" FontSize="32" FontWeight="Bold" Foreground="#6B7280" Margin="10,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>


        <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Background="White">
            <DataGrid ItemsSource="{Binding Orders}" AutoGenerateColumns="False" 
      Background="Transparent" BorderThickness="0" RowHeight="50"
      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
      HorizontalGridLinesBrush="#EEEEEE" CanUserAddRows="False" IsReadOnly="True">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#D1D5DB"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Width="150"/>

                    <DataGridTemplateColumn Header="КУРЬЕР" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="" VerticalAlignment="Center" Foreground="Black" Margin="12,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                   

                    <DataGridTemplateColumn Header="ТРАНСПОРТ" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#E0E7FF" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                    <TextBlock Foreground="Black" FontSize="11" FontWeight="Medium"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="СТАТУС" Width="200"/>
                    <DataGridTextColumn Header="ЗАКАЗОВ"  Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
