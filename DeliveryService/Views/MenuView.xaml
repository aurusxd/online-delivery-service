<Window x:Class="DeliveryService.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MenuView" Height="800" Width="1200" WindowStartupLocation="CenterScreen">
    

    <Grid Background="#F9FAFB" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="24" Padding="25" Margin="0,0,15,0" Background="White">
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Меню" FontSize="28" FontWeight="Bold" Margin="5,0,0,15" Foreground="#111827"/>

                <WrapPanel Grid.Row="1" Margin="5,0,0,15">
                    <Button Content="Все" Command="{Binding LoadMenuItemsCommand}" CommandParameter="{x:Null}"
                            Style="{StaticResource CategoryButtonStyle}"/>
                    <ItemsControl ItemsSource="{Binding Categories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Name}" 
                                    Command="{Binding DataContext.LoadMenuItemsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding Id}"
                                    Style="{StaticResource CategoryButtonStyle}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </WrapPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ListView ItemsSource="{Binding MenuItems}" 
                              Background="Transparent" 
                              BorderThickness="0"
                              HorizontalContentAlignment="Left"
                              PreviewMouseWheel="ListView_PreviewMouseWheel">

                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollContentPresenter}}" 
                                           HorizontalAlignment="Left"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Width="190" Height="290" Background="White" BorderBrush="#F3F4F6" BorderThickness="1" CornerRadius="24" Margin="7">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="130"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="18" Margin="12,12,12,0" ClipToBounds="True">
                                            <Image Source="{Binding ImagePath}" Stretch="UniformToFill"/>
                                        </Border>
                                        <StackPanel Grid.Row="1" Margin="15,10,15,0">
                                            <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="Bold" Foreground="#111827"/>
                                            <TextBlock Text="{Binding Weight}" FontSize="12" Foreground="#9CA3AF"/>
                                        </StackPanel>
                                        <Grid Grid.Row="2" Margin="15,0,12,15" VerticalAlignment="Bottom">
                                            <TextBlock Text="{Binding Price}" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
                                            <Button Content="Добавить" 
                                                    Command="{Binding DataContext.AddToBasketCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding Id}"
                                                    Style="{StaticResource PrimaryButton}" Height="32" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Column="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="24" Padding="25" Background="White">
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Корзина" FontSize="28" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding BasketItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="10,5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Food.Title}" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text="{Binding Quantity}" Foreground="#6B7280" FontSize="12"/>
                                            <TextBlock Text="{Binding Food.Price}" Foreground="#6B7280" FontSize="12"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="✕" 
                                            Command="{Binding DataContext.RemoveFromBasketCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Id}"
                                            Background="Transparent" BorderThickness="0" Foreground="#EF4444" Cursor="Hand"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Button Grid.Row="2" Content="Сделать заказ" Style="{StaticResource PrimaryButton}" Height="50" Margin="0,20,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>