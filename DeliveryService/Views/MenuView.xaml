<Window x:Class="DeliveryService.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeliveryService.Views"
        mc:Ignorable="d"
        Title="MenuView" Height="800" Width="1200">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="450"/>
        </Grid.ColumnDefinitions>


        <Border Grid.Column="0" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,0,10,0"
                Background="White">

            <StackPanel>
                <TextBlock Text="Меню" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Margin="0,0,0,15"
                           Foreground="#2C3E50"/>


                <TextBlock 
                           FontSize="14" 
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           Margin="0,50,0,0"/>
            </StackPanel>
        </Border>


        <Border Grid.Column="1" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="15"
                Background="White"
                Width="450">

            <StackPanel>
                <TextBlock Text="Корзина" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Margin="0,0,0,15"
                           Foreground="#2C3E50"/>


                <TextBlock  
                           FontSize="14" 
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           Margin="0,50,0,0"/>
                <Button Content="Перейти к оплате" 
                        FontSize="16" 
                        FontWeight="Bold"
                        Height="40"
                        Width="250"
                        Style="{StaticResource ResourceKey=PrimaryButton}" Margin="0,535,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
